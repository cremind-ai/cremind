"""A2A remote-agent (outbound) tool — REMOVED.

Stubbed empty when the agent moved to a single-model native function-calling
loop; outbound A2A is no longer a tool type. Delete ``app/tools/a2a`` from
version control (``git rm -r app/tools/a2a``).
"""
