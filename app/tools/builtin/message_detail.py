"""Message Detail built-in tool — REMOVED.

The model now receives full conversation history natively, so fetching messages
by id is unnecessary. Delete this file from version control
(``git rm app/tools/builtin/message_detail.py``).
"""
